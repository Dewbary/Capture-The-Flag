// import { PlayerInfo } from "../../types";

// export const playerJoined = (player: PlayerInfo) =>
//   ACTIONS.playerJoined({ player });

// export const playerLeft = (playerId: string) =>
//   ACTIONS.playerLeft({ playerId });

// export const playerMoved = (
//   playerId: string,
//   position: { x: number; y: number }
// ) => ACTIONS.playerMoved({ playerId, position });

// export const flagCaptured = (teamId: "red" | "blue") =>
//   ACTIONS.flagCaptured({ teamId });

// export const flagDropped = (
//   teamId: "red" | "blue",
//   position: { x: number; y: number }
// ) => ACTIONS.flagDropped({ teamId, position });

// export const teamScored = (teamId: "red" | "blue") =>
//   ACTIONS.teamScored({ teamId });
